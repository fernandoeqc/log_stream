name: set-connection
  payload:
    type: object
    properties:
      msgtype:
        type: string
        enum:
          - set-connection
      mode:
        type: string
        enum:
          - WIFI
    required:
      - msgtype
      - mode|
